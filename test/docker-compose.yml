version: '3'
services:
    cloudflared:
        image: klutchell/cloudflared
    dig:
        image: alpine
        command: sh -c 'apk add -q --no-cache bind-tools && dig @cloudflared cloudflare.com AAAA'
        links:
            - cloudflared