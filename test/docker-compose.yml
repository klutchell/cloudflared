version: '3'
services:
    cloudflared:
        image: ${DOCKER_REPO}
    drill:
        image: alpine
        command: sh -c 'apk add -q --no-cache drill && drill -p 5053 cloudflare.com @cloudflared'
        links:
            - cloudflared